@model Intranet.Areas.Elements_Generaux.Models.Fraction

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Voir le détail d'une fraction</title>
</head>
<body>
    <div>
        <h4>Fraction</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Libelle)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Libelle)
            </dd>

        </dl>
    </div>
    <p>
        @Html.ActionLink("Modifier", "Fraction", "Modifier", new { id = Model.Element.Id }, null)|
        @Html.ActionLink("Revenir sur la liste des fractions", "Fractions", "Afficher", null, null)
    </p>
</body>
</html>